# -----------------------------------------------------------------------------
# GCZ-MAIN - Beispiel für Umgebungsvariablen
#
# Kopieren Sie diese Datei nach .env und ersetzen Sie die Platzhalterwerte
# mit Ihren eigenen Konfigurationen.
# Die .env-Datei sollte niemals in Git eingecheckt werden.
# -----------------------------------------------------------------------------

# --- Allgemeine Konfiguration ---
# Log-Level für die Anwendung (z.B. DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Pfad zu einer spezifischen YAML-Konfigurationsdatei, falls benötigt
# GCZ_CONFIG_PATH=configs/custom.yml

# --- Ollama / Lokale LLMs ---
# Vollständige URL zum Ollama-Server
OLLAMA_HOST=http://localhost:11434

# Standard-Modell, das von den Agenten verwendet wird, falls nicht anders angegeben
DEFAULT_MODEL=llama3:latest

# --- ComfyUI / Stable Diffusion Service ---
# Adresse und Port der ComfyUI API
COMFYUI_URL=127.0.0.1:8188

# Optional: Absoluter Pfad zur ComfyUI-Installation (wird von einigen Skripten benötigt)
# COMFYUI_PATH=C:/path/to/ComfyUI

# Optional: Setzen Sie diesen Wert auf "1", um das Frontend des ComfyUI-Managers zu deaktivieren
# DISABLE_COMFYUI_MANAGER_FRONT=0

# --- E-Mail Service ---
# Anmeldeinformationen für das Abrufen von E-Mails.
# Das System ist so konzipiert, dass es verschiedene Provider unterstützt.
# Hier sind Beispiele für GMail und einen generischen IMAP-Server.
# Füllen Sie die für Sie relevanten Werte aus.

# Für GMail (erfordert ein App-Passwort, nicht Ihr reguläres Passwort)
GMAIL_USER=deine-email@gmail.com
GMAIL_PASS=dein-gmail-app-passwort

# Für einen generischen IMAP-Server
IMAP_HOST=imap.dein-provider.de
IMAP_USER=dein-imap-benutzer
IMAP_PASS=dein-imap-passwort

# --- Telegram Bot ---
# API-Token für Ihren Telegram Bot (erhalten von @BotFather)
TELEGRAM_BOT_TOKEN=DEIN_TELEGRAM_BOT_TOKEN

# --- Externe APIs & Dienste ---
# API-Schlüssel für OpenAI (falls verwendet)
OPENAI_API_KEY=sk-DEIN_OPENAI_API_KEY

# Optional: GitHub-Token für den Zugriff auf die GitHub-API (wird von einigen Tools benötigt)
# GITHUB_TOKEN=DEIN_GITHUB_TOKEN

# Optional: Anmeldeinformationen für den Instagram-Service
# IG_USERNAME=dein_instagram_benutzername
# IG_PASSWORD=dein_instagram_passwort

# Optional: Pfad zur client_secret.json-Datei für den YouTube-Service
# YOUTUBE_CLIENT_SECRETS_FILE=client_secret.json

# --- Hardware & Performance ---
# Geben Sie an, welche(s) GPU(s) für CUDA-Operationen verwendet werden soll(en) (z.B., 0 oder 0,1)
# CUDA_VISIBLE_DEVICES=0

# Pfad zur ffmpeg-Executable (wichtig für die Videoverarbeitung)
PATH_FFMPEG=ffmpeg

# --- KI-Modell-Pfade & Konfigurationen ---
# Setzen Sie diesen Wert auf "1", um den Nutzungsbedingungen von Coqui TTS zuzustimmen
COQUI_TOS_AGREED=0

# Optional: Spezifisches Verzeichnis für TTS-Modelle
# TTS_HOME=/pfad/zu/deinen/tts/modellen

# Optional: Spezifisches Verzeichnis für Tortoise-TTS-Modelle
# TORTOISE_MODELS_DIR=/pfad/zu/deinen/tortoise/modellen

# Optional: Hugging Face Endpunkt
# HF_ENDPOINT=https://huggingface.co
