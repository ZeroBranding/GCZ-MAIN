# -----------------------------------------------------------------------------
# GCZ-MAIN - Environment Variable Template
#
# Copy this file to .env and fill in your actual configuration values.
# The .env file is ignored by Git and should never be committed.
# -----------------------------------------------------------------------------

# --- General & Core ---
# Log level for the application (e.g., DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Path to a specific YAML configuration file.
# GCZ_CONFIG_PATH=configs/custom.yml

# --- Telegram Bot ---
# Your Telegram Bot API Token from @BotFather.
# This is REQUIRED to run the bot.
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# --- Ollama / Local LLMs ---
# The full URL of your Ollama server.
OLLAMA_HOST=http://localhost:11434

# The default model to be used by agents if not otherwise specified.
DEFAULT_MODEL=llama3:latest

# --- ComfyUI / Stable Diffusion Service ---
# The address and port of the ComfyUI API.
COMFYUI_URL=127.0.0.1:8188

# Optional: Absolute path to your ComfyUI installation.
# COMFYUI_PATH=C:/path/to/ComfyUI

# Optional: Set to "1" to disable the ComfyUI-Manager frontend.
# DISABLE_COMFYUI_MANAGER_FRONT=0

# --- External APIs & Services ---
# API key for OpenAI (if used).
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY

# Optional: GitHub token for accessing the GitHub API (used by some tools).
# GITHUB_TOKEN=YOUR_GITHUB_TOKEN

# --- Email Service ---
# Credentials for polling email. The system supports various providers.
# Fill in the values for the provider you are using.

# Example for GMail (requires an App Password, not your regular password)
# GMAIL_USER=your-email@gmail.com
# GMAIL_PASS=your-gmail-app-password

# Example for a generic IMAP server
# IMAP_HOST=imap.your-provider.com
# IMAP_USER=your-imap-user
# IMAP_PASS=your-imap-password
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587

# --- Social Media Services (Optional) ---
# IG_USERNAME=your_instagram_username
# IG_PASSWORD=your_instagram_password
# YOUTUBE_CLIENT_SECRETS_FILE=client_secret.json

# --- Hardware & Performance ---
# Specify which GPU(s) to use for CUDA operations (e.g., 0 or 0,1).
# CUDA_VISIBLE_DEVICES=0

# Path to the ffmpeg executable (important for video processing).
PATH_FFMPEG=ffmpeg

# --- Media & Artifact Handling ---
# Set to "0" to disable saving media artifacts locally.
SAVE_MEDIA_LOCALLY=1

# Set to "0" to prevent the bot from sending generated images to Telegram.
TELEGRAM_SEND_IMAGES=1

# Set to "0" to prevent the bot from sending generated videos to Telegram.
TELEGRAM_SEND_VIDEOS=1
